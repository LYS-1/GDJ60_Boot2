<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.iu.base.member.MemberDAO">
	<insert id="setRoleInsert" parameterType="MemberVO">
		INSERT INTO MEMBER_ROLE (USERNAME, NUM) 
		VALUES(#{userName},3)
   	</insert> 
   	<insert id="setMemberRole" parameterType="Map">
   		INSERT INTO MEMBER_ROLE (USERNAME, NUM) 
   		VALUES(#{userName},#{num})
   	</insert>
   	<insert id="setJoin" parameterType="MemberVO" useGeneratedKeys="true" keyProperty="userName">
      INSERT INTO MEMBER
      VALUES(#{userName},#{password},#{name},#{email},#{birth},1)
   	</insert>
	
	<select id="getCheckId" parameterType="memberVO" resultType="String">
		SELECT COUNT(USERNAME) FROM MEMBER WHERE USERNAME = #{userName}
	</select>
	
	<insert id="setInsert" parameterType="memberVO">
	INSERT INTO MEMBER (USERNAME, PASSWORD, EMAIL,BIRTH,ENABLED)
	VALUES(#{userName},#{password},#{email},#{birth},1)
	</insert> 

	<select id="getLogin" parameterType="MemberVO" resultMap="GetLoglnResult">
	 SELECT M.userName, M.email, R.num , R.ROLENAME
	 FROM MEMBER M 
	 	INNER JOIN 
	 	MEMBER_ROLE MR
	 	ON M.USERNAME = MR.USERNAME
	 	INNER JOIN
	 	ROLE R
	 	ON MR.NUM = R.NUM
	 	WHERE M.USERNAME = #{userName} AND M.PASSWORD = #{password}
	
	</select>
	
	<resultMap type="MemberVO" id="GetLoglnResult">
		<id property="userName" column="userName"/>
		<result property="email" column="email"/>
		<collection property="roleVOs" javaType="List" ofType="MemberRoleVO">
			<id column="num" property="num" />
			<result column="RoleName"  property="RoleName"/>
		</collection>
	
	</resultMap>
	<resultMap type="MemberVO" id="memberLogin">
	    <id column="USERNAME" property="userName"/>
	    <result column="PASSWORD" property="password"/>
	    <result column="NAME" property="name"/>
	    <result column="EMAIL" property="email"/>
	    <result column="BIRTH" property="birth"/>
	    <result column="ENABLED" property="enabled"/>
	    <!-- <association property="roleVO" javaType="RoleVO">
	    <id column="NUM" property="num"/>
	    <result column="ROLENAME" property="roleName"/>
	    </association> -->
	    <collection property="roleVOs" javaType="List" ofType="MemberRoleVO">
		    <id column="NUM" property="num"/>
		    <result column="ROLENAME" property="roleName"/>
	    </collection>
    </resultMap>
	
</mapper>